<section class="section section-dark" id="gallery">
  <div class="container">
    <div class="section-label">Gallery</div>
    <h2 class="section-title">Our Work</h2>
    <p class="section-sub">A glimpse into the craftsmanship and creativity of our members.</p>
    <div class="gallery-preview">
      {Array.from({ length: 16 }, (_, i) => (
        <div class="gallery-item">
          <img src={`/gallery/thumb_${String(i + 1).padStart(2, '0')}.jpg`} alt="Woodturning project" loading="lazy" />
        </div>
      ))}
    </div>
  </div>
</section>

<style is:global>
  .gallery-preview {
    display: grid; grid-template-columns: repeat(4, 1fr); gap: 0.8rem;
  }
  .gallery-item {
    aspect-ratio: 1; border-radius: 10px; overflow: hidden;
    position: relative; cursor: pointer;
  }
  .gallery-item img {
    width: 100%; height: 100%; object-fit: cover;
    transition: transform 0.5s ease, filter 0.5s ease;
  }
  .gallery-item:hover img {
    transform: scale(1.08);
    filter: brightness(1.1);
  }
  @media (max-width: 768px) {
    .gallery-preview { grid-template-columns: repeat(2, 1fr); }
  }
</style>
